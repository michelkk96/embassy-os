// prettier-ignore
/**
 * French (fr_FR) UI dictionary: numeric id -> translated string.
 * Empty scaffold — populate against the ids in `./en.ts` to translate.
 */
const fr: Record<number, string> = {}

export default fr
