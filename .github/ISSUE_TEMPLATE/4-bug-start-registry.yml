name: 🗄️ Start Registry — Bug Report
description: A defect in the registry server (`registrybox`) — package/OS indexes, assets, or signing.
type: Bug
labels: [start-registry]
assignees:
  - dr-bonez
body:
  - type: markdown
    attributes:
      value: |
        **Filing this with an agent?** `gh issue create` bypasses this form entirely — pass the metadata yourself with `--type Bug --label start-registry`, and mirror the `###` headings below as your body's sections. Title the issue as the *finding*, not the symptom alone: `<what breaks> — <why> (<file:line>)`.

        A package that is *missing or broken in a registry* is an issue for that package's own repo, not here. This form is for the registry server itself.

  - type: checkboxes
    id: prerequisites
    attributes:
      label: Prerequisites
      options:
        - label: I searched [existing issues](https://github.com/Start9Labs/start-technologies/issues?q=is%3Aissue) and this is not already reported.
          required: true

  - type: input
    id: registry
    attributes:
      label: Which registry?
      description: The URL you were talking to, or "self-hosted" plus how you deployed it.
      placeholder: registry.start9.com
    validations:
      required: true

  - type: dropdown
    id: area
    attributes:
      label: Which surface?
      options:
        - Package index
        - OS index / version selection
        - Asset upload
        - Asset download
        - Signing & signature verification
        - Admins & signers
        - Metrics
        - JSON-RPC API
        - Database / migration
        - Other / unsure
    validations:
      required: true

  - type: dropdown
    id: client
    attributes:
      label: How were you talking to it?
      options:
        - StartOS marketplace tab
        - marketplace.start9.com (brochure)
        - start-cli registry commands
        - Direct HTTP / JSON-RPC
        - Other
    validations:
      required: true

  - type: input
    id: version
    attributes:
      label: registrybox version
      description: Only for a self-hosted registry — the release you deployed, or the git hash if you built it.
      placeholder: 0.4.0.2

  - type: textarea
    id: current
    attributes:
      label: What happens
      description: Include the request and the response — status code, RPC error, or the wrong data returned.
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: What should happen instead
    validations:
      required: true

  - type: textarea
    id: repro
    attributes:
      label: Steps to reproduce
      description: If you found this by reading the code rather than hitting it, leave this blank and fill in **Root cause** below instead.

  - type: textarea
    id: root-cause
    attributes:
      label: Root cause / code pointers
      description: If you traced it, name the file, the line, and the mechanism. `registrybox` is a thin wrapper — the server logic is in `shared-libs/crates/start-core/src/registry/`.
      placeholder: |
        `shared-libs/crates/start-core/src/registry/...` — …

  - type: textarea
    id: logs
    attributes:
      label: Logs & evidence
      render: shell
