// prettier-ignore
/**
 * German (de_DE) UI dictionary: numeric id -> translated string.
 * Empty scaffold — populate against the ids in `./en.ts` to translate.
 */
const de: Record<number, string> = {}

export default de
