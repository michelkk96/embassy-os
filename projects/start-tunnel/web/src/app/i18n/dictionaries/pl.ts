// prettier-ignore
/**
 * Polish (pl_PL) UI dictionary: numeric id -> translated string.
 * Empty scaffold — populate against the ids in `./en.ts` to translate.
 */
const pl: Record<number, string> = {}

export default pl
