# book-name=version
# Adding a book here is all that's needed — build.sh, deploy.yml,
# and nginx routing all derive from this file automatically.
start-os=0.4.0.x
start-tunnel=1.0.x
packaging=0.4.0.x
bitcoin-guides=1.0.x
# start-wrt not yet ready for publication — kept out of the published site.
# Uncomment to re-enable (drives build.sh, docs-deploy.yml, nginx routing, and llms.txt).
# start-wrt=0.1.0.x
