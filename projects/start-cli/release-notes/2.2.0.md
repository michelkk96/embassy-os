**start-cli 2.2.0 makes emulated service images portable across CPU architectures.**

## Highlights

- **`s9pk edit add-image` enables CPU emulation by default** when a server uses another architecture. Use `--no-emulation` for images that require a native architecture. StartOS selects the image the s9pk contains, including one retained in a backup.
